# 毎日のブログ記事拡張タスク(Claude Code /schedule 用)

## 目的
`assets/blog-plan-190.json` に残っている「未執筆」の地域×業界記事を、
中身の薄いテンプレート記事ではなく、実際に内容のあるブログ記事として
毎日少しずつ追加していく。

## 毎回の実行手順

1. `assets/cities.json` の並び順で、`assets/city-context.json` に
   まだ登録されていない市を1つ選ぶ。

2. 選んだ市について、以下の観点を踏まえた背景説明(3〜4文、日本語)を
   自分で調べて書き、`assets/city-context.json` に追加する。
   - 主要な産業構造(`assets/cities.json` の `industry` フィールドが手がかりになる)
   - 人口規模・都市としての位置づけ(県庁所在地への近さ、ベッドタウンか otherwise か等)
   - 採用市場の特徴として言えそうなこと
   **注意: 具体的な統計数値(人口の実数、有効求人倍率の数値など)を
   確認できない場合は書かない。一般的に知られている傾向の範囲で書くこと。**

3. `node build-blog.js` を実行する。
   → 追加した市 × 10業界の記事が `pages/blog/` に自動生成される
     (テンプレートは `template-blog-combo.html`、
      業界ごとの原因・改善策は `assets/industries.json` を使用)。

4. 生成された記事を1〜2本、実際に開いて内容を確認する。
   日本語として不自然な箇所、事実と異なりそうな記述がないか目視でチェックする。

5. `node build-blog-index.js` を実行し、ブログ一覧ページを更新する。

6. `node update-plan-status.js` を実行し、
   `assets/blog-plan-190.json` のステータスを更新する。

7. 変更内容をコミットしてpushする(例: `git add -A && git commit -m "○○市の記事10本を追加" && git push`)。

8. 変更内容を簡潔にまとめて報告する(例: 「○○市の10記事を追加。残り○件」)。

## やってはいけないこと

- 複数の市を1回でまとめて処理しない(1日1市までとし、品質を優先する)
- `assets/city-context.json` に実在しない・確認できない統計数値を書かない
- 生成された記事の目視確認(手順4)を省略しない
- `assets/blog-plan-190.json` の未執筆件数が0になったら、このタスクは終了してよい

## 実行頻度の目安

1日1回。残り16市なので、約16日で `blog-plan-190.json` の
地域×業界の記事(190件)がすべて出揃う計算になる。
